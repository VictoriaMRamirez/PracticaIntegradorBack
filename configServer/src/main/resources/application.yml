server:
  port: ${PORT:8888}

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/jonathanvictoricadh/esp-back-1-202210-integrador #subir al repo esto y ponerle el link de github
          default-label: main
          force-pull: true
          search-paths: configData
